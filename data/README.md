# 원본 데이터 배치 안내

이 폴더에는 공공데이터포털에서 제공하는 `국토교통부_전국교량표준데이터` CSV 파일을 배치합니다.

원본 CSV는 용량과 재현성 관리를 위해 기본적으로 GitHub 저장소에 올리지 않습니다. 공공데이터포털에서 CSV를 다운로드한 뒤 아래 경로로 저장하세요.

```text
data/bridge_data.csv
```

분석 스크립트는 현재 폴더와 하위 폴더의 CSV를 자동 탐색합니다.

```bash
python bridge_ai_analysis.py
```

